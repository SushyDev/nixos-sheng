# No ExecStartPre delay: libssc retries the SSC lookup itself, so the wait for
# sensorspd to enumerate on QRTR costs only as long as it actually takes.
[Unit]
Wants=adsprpcd-sensorspd.service
After=adsprpcd-sensorspd.service
